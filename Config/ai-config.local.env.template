# =============================================================================
# TEMPLATE Azure OpenAI Configuration
# =============================================================================
# This template provides the required environment variables for the migration
# agents. Copy it to ai-config.local.env and replace the placeholder values with
# your actual credentials and settings. Do not commit the populated file.
# =============================================================================

# Azure OpenAI Service Configuration example
AZURE_OPENAI_ENDPOINT="https://g-openai.azure.com/"
AZURE_OPENAI_API_KEY="abcde3299bd143ad8920e04168d78e95"

# Model Deployment Configuration
AZURE_OPENAI_DEPLOYMENT_NAME="your-deployment-name"
AZURE_OPENAI_MODEL_ID="gpt-4.1"

# Specialized Model IDs (can be different models for different tasks)
AZURE_OPENAI_COBOL_ANALYZER_MODEL="gpt-4.1"
AZURE_OPENAI_JAVA_CONVERTER_MODEL="gpt-4.1"
AZURE_OPENAI_DEPENDENCY_MAPPER_MODEL="gpt-4.1"
AZURE_OPENAI_UNIT_TEST_MODEL="gpt-4.1"

# Service Configuration
AZURE_OPENAI_SERVICE_TYPE="AzureOpenAI"
AZURE_OPENAI_MAX_TOKENS="32000"
AZURE_OPENAI_TEMPERATURE="0.1"

# Application Settings
COBOL_SOURCE_FOLDER="source"
JAVA_OUTPUT_FOLDER="output"
CSHARP_OUTPUT_FOLDER="output"
TEST_OUTPUT_FOLDER="TestOutput"
TARGET_LANGUAGE="Java"

# Logging Configuration
LOG_LEVEL="Information"
ENABLE_CHAT_LOGGING="true"
ENABLE_API_CALL_LOGGING="true"

# =============================================================================
# Setup Instructions:
# =============================================================================
# 1. Retrieve your Azure OpenAI credentials from the Azure Portal.
# 2. Replace the placeholder values above with your actual configuration.
# 3. Save the file as Config/ai-config.local.env.
# 4. Test your configuration by running: ./doctor.sh setup && ./doctor.sh test
# =============================================================================
